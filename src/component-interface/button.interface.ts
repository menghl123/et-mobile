export interface ButtonInterface {
  block: boolean; // display = block
  clear: boolean; // 没有边框
  type: string; // primary secondary danger light dark
  outline: boolean; // 有边框，没有背景色
  round: boolean; // 圆角按钮
  size: string; // default large small
}
